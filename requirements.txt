ccxt>=4.2.0
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
# Forecast layer (optional for structure-only/dry tests, required for live bias):
torch>=2.2.0
chronos-forecasting @ git+https://github.com/amazon-science/chronos-forecasting.git
# Optional: news sentiment (only needed when ENABLE_NEWS_SENTIMENT=true):
transformers>=4.40.0
